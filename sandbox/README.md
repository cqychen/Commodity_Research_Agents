# 沙箱实验目录

本目录用于 Agent 自动生成和执行实验代码。沙箱的目标是验证新特征、新模型或新评分规则，不允许直接修改正式项目代码。

安全边界：

- 只能写入 `sandbox/runs/[run_id]/`。
- 不允许读取或输出 API key。
- 不允许自动下单。
- 不允许删除正式项目文件。
- 实验通过后只能生成 `promotion_proposal.md`，是否合并由人工确认。
