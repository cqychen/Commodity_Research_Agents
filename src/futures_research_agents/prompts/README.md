# 中文 Prompt 模板

本目录保存每个 Agent 的中文 Prompt。Prompt 负责研究判断、解释、反证、实验方案和报告表达；标签构造、模型训练、回测、模型固化和沙箱执行仍由 Python 工具函数完成。

每个 Prompt 统一包含：

- 角色
- 输入
- 任务
- 输出格式
- 约束
- 禁止事项
- 质量检查

节点执行时可以通过 `prompt_loader.py` 读取模板，并注入当前 `state`、品种配置和流程文档摘要。没有 LLM 依赖或没有 API key 时，系统会使用规则节点兜底，保证工作流仍可运行。
